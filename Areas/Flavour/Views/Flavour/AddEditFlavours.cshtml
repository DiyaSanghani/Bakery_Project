﻿@using System.Data;
@using Bakery_Project.Areas.Flavour.Models;
@model Bakery_Project.Areas.Flavour.Models.FlavourModel;
@{
    string opration = "";
    if (ViewBag.ID > 0)
    {
        opration = "Edit";
    }
    else
    {
        opration = "Add";
    }
}


<div class="row ">
    <div class="col-lg-8 align-items-center">

        <div class="card">
            <div class="card-body">
                <h5 class="card-title">@opration Record</h5>
                <!-- Multi Columns Form -->
                <form class="row g-3" asp-action="AddEditFlavoursData" method="post">
                    @Html.HiddenFor(x => x.FlavourID)
                    <div class="col-md-12">
                        <label for="inputName5" class="form-label">Flavour Name</label>
                        <input asp-for="FlavourName" type="text" class="form-control">
                        <span asp-validation-for="FlavourName" class="text-danger"></span>
                    </div>

                    <div class="col-md-12">
                        <label for="inputName5" class="form-label">Flavour Description</label>
                        <input asp-for="FlavourDescription" type="text" class="form-control">
                        <span asp-validation-for="FlavourDescription" class="text-danger"></span>
                    </div>


                    <div class="text-left">
                        <button type="submit" class="btn btn-primary">@opration Record</button>
                        <button asp-action="Cancel" asp-area="Flavour" asp-controller="Flavour" class="btn btn-primary">Cancel</button>
                    </div>
                </form><!-- End Multi Columns Form -->
            </div>
        </div>
    </div>
</div>